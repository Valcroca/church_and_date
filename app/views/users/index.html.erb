<h1>User#index</h1>
<p>Find me in app/views/user/index.html.erb</p>

<%= button_to 'Sign Out', destroy_user_session_path, method: :delete %>

<% if @profile %>
  <%= link_to 'Your Profile', user_profiles_path(@user.id) %>
<% else %>
  <%= link_to 'Create Profile', new_user_profile_path(@user.id) %>
<% end %>